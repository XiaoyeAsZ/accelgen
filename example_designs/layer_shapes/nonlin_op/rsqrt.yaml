{{include_text('../problem_base.yaml')}}

# ===========================================================================
# math.rsqrt  —  Reciprocal square root
# ===========================================================================
# MLIR:  %r = math.rsqrt %x : bf16/f32
# Hardware: Newton-Raphson iterations on PPU
#   Initial guess + 2 refinement iterations, each = 1 MAC
# C = 3
# ===========================================================================
problem:
  <<<: *problem_base
  instance:
    C: 3
