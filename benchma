#map = affine_map<(d0, d1) -> (d1, d0)>
#map1 = affine_map<(d0, d1) -> (d0, d1)>
#map2 = affine_map<(d0, d1, d2) -> (d1, d2)>
#map3 = affine_map<(d0, d1, d2) -> (d0, d1, d2)>
#map4 = affine_map<(d0, d1, d2) -> ()>
#map5 = affine_map<(d0, d1, d2, d3) -> (d0, d1, d3)>
#map6 = affine_map<(d0, d1, d2, d3) -> (d0, d3, d2)>
#map7 = affine_map<(d0, d1, d2, d3) -> (d0, d1, d2)>
#map8 = affine_map<(d0, d1, d2, d3) -> (d0, d2, d1, d3)>
#map9 = affine_map<(d0, d1, d2, d3) -> (d0, d1, d2, d3)>
#map10 = affine_map<(d0, d1, d2, d3) -> (0, 0, 0, d3)>
#map11 = affine_map<(d0, d1, d2, d3, d4) -> (d0, d1, d3, d4)>
#map12 = affine_map<(d0, d1, d2, d3, d4) -> (d0, d1, d2, d3, d4)>
#map13 = affine_map<(d0, d1, d2, d3) -> (d0, d1, d3, d2)>
#map14 = affine_map<(d0, d1, d2, d3) -> (0, d1, 0, 0)>
#map15 = affine_map<(d0, d1, d2, d3) -> (d0, d1, d2, 0)>
#map16 = affine_map<(d0, d1, d2, d3) -> ()>
module {
  func.func @main(%arg0: memref<8x1024x4096xbf16, strided<[?, ?, ?], offset: ?>>, %arg1: memref<1x1x128xbf16, strided<[?, ?, ?], offset: ?>>, %arg2: memref<1x1x128xbf16, strided<[?, ?, ?], offset: ?>>, %arg3: memref<1x32x1x1xbf16, strided<[?, ?, ?, ?], offset: ?>>, %arg4: memref<4096x4096xbf16, strided<[?, ?], offset: ?>>, %arg5: memref<1024x4096xbf16, strided<[?, ?], offset: ?>>, %arg6: memref<1024x4096xbf16, strided<[?, ?], offset: ?>>, %arg7: memref<4096x4096xbf16, strided<[?, ?], offset: ?>>) -> (memref<8x1024x4096xbf16>, memref<8x32x1024x1024xbf16>) {
    %c0_i64 = arith.constant 0 : i64
    %cst = arith.constant 0.000000e+00 : bf16
    %cst_0 = arith.constant 0xFF800000 : f32
    %cst_1 = arith.constant 0.000000e+00 : f32
    %cst_2 = arith.constant 0.088388347648318447 : f64
    %alloc = memref.alloc() {alignment = 64 : i64} : memref<4096x4096xbf16>
    linalg.generic {indexing_maps = [#map, #map1], iterator_types = ["parallel", "parallel"]} ins(%arg4 : memref<4096x4096xbf16, strided<[?, ?], offset: ?>>) outs(%alloc : memref<4096x4096xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_3 = memref.alloc() {alignment = 64 : i64} : memref<8x4096x4096xbf16>
    linalg.generic {indexing_maps = [#map2, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%alloc : memref<4096x4096xbf16>) outs(%alloc_3 : memref<8x4096x4096xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_4 = memref.alloc() {alignment = 64 : i64} : memref<8x1024x4096xbf16>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%cst : bf16) outs(%alloc_4 : memref<8x1024x4096xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_5 = memref.alloc() {alignment = 64 : i64} : memref<8x1024x4096xbf16>
    memref.copy %alloc_4, %alloc_5 : memref<8x1024x4096xbf16> to memref<8x1024x4096xbf16>
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%arg0, %alloc_3 : memref<8x1024x4096xbf16, strided<[?, ?, ?], offset: ?>>, memref<8x4096x4096xbf16>) outs(%alloc_5 : memref<8x1024x4096xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %expand_shape = memref.expand_shape %alloc_5 [[0], [1], [2, 3]] output_shape [8, 1024, 32, 128] : memref<8x1024x4096xbf16> into memref<8x1024x32x128xbf16>
    %alloc_6 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x128xbf16>
    linalg.generic {indexing_maps = [#map8, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%expand_shape : memref<8x1024x32x128xbf16>) outs(%alloc_6 : memref<8x32x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_7 = memref.alloc() {alignment = 64 : i64} : memref<4096x1024xbf16>
    linalg.generic {indexing_maps = [#map, #map1], iterator_types = ["parallel", "parallel"]} ins(%arg5 : memref<1024x4096xbf16, strided<[?, ?], offset: ?>>) outs(%alloc_7 : memref<4096x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_8 = memref.alloc() {alignment = 64 : i64} : memref<8x4096x1024xbf16>
    linalg.generic {indexing_maps = [#map2, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%alloc_7 : memref<4096x1024xbf16>) outs(%alloc_8 : memref<8x4096x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_9 = memref.alloc() {alignment = 64 : i64} : memref<8x1024x1024xbf16>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%cst : bf16) outs(%alloc_9 : memref<8x1024x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_10 = memref.alloc() {alignment = 64 : i64} : memref<8x1024x1024xbf16>
    memref.copy %alloc_9, %alloc_10 : memref<8x1024x1024xbf16> to memref<8x1024x1024xbf16>
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%arg0, %alloc_8 : memref<8x1024x4096xbf16, strided<[?, ?, ?], offset: ?>>, memref<8x4096x1024xbf16>) outs(%alloc_10 : memref<8x1024x1024xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %expand_shape_11 = memref.expand_shape %alloc_10 [[0], [1], [2, 3]] output_shape [8, 1024, 8, 128] : memref<8x1024x1024xbf16> into memref<8x1024x8x128xbf16>
    %alloc_12 = memref.alloc() {alignment = 64 : i64} : memref<8x8x1024x128xbf16>
    linalg.generic {indexing_maps = [#map8, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%expand_shape_11 : memref<8x1024x8x128xbf16>) outs(%alloc_12 : memref<8x8x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map, #map1], iterator_types = ["parallel", "parallel"]} ins(%arg6 : memref<1024x4096xbf16, strided<[?, ?], offset: ?>>) outs(%alloc_7 : memref<4096x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map2, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%alloc_7 : memref<4096x1024xbf16>) outs(%alloc_8 : memref<8x4096x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%arg0, %alloc_8 : memref<8x1024x4096xbf16, strided<[?, ?, ?], offset: ?>>, memref<8x4096x1024xbf16>) outs(%alloc_9 : memref<8x1024x1024xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %expand_shape_13 = memref.expand_shape %alloc_9 [[0], [1], [2, 3]] output_shape [8, 1024, 8, 128] : memref<8x1024x1024xbf16> into memref<8x1024x8x128xbf16>
    %alloc_14 = memref.alloc() {alignment = 64 : i64} : memref<8x8x1024x128xbf16>
    linalg.generic {indexing_maps = [#map8, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%expand_shape_13 : memref<8x1024x8x128xbf16>) outs(%alloc_14 : memref<8x8x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %expand_shape_15 = memref.expand_shape %arg1 [[0], [1, 2], [3]] output_shape [1, 1, 1, 128] : memref<1x1x128xbf16, strided<[?, ?, ?], offset: ?>> into memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>
    %expand_shape_16 = memref.expand_shape %arg2 [[0], [1, 2], [3]] output_shape [1, 1, 1, 128] : memref<1x1x128xbf16, strided<[?, ?, ?], offset: ?>> into memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>
    %alloc_17 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x128xbf16>
    linalg.generic {indexing_maps = [#map9, #map10, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_6, %expand_shape_15 : memref<8x32x1024x128xbf16>, memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>) outs(%alloc_17 : memref<8x32x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    %subview = memref.subview %alloc_6[0, 0, 0, 0] [8, 32, 1024, 64] [1, 1, 1, 1] : memref<8x32x1024x128xbf16> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1]>>
    %subview_18 = memref.subview %alloc_6[0, 0, 0, 64] [8, 32, 1024, 64] [1, 1, 1, 1] : memref<8x32x1024x128xbf16> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1], offset: 64>>
    %alloc_19 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x64xbf16>
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%subview_18 : memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1], offset: 64>>) outs(%alloc_19 : memref<8x32x1024x64xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      %0 = arith.negf %in : bf16
      linalg.yield %0 : bf16
    }
    %alloc_20 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x128xbf16>
    %subview_21 = memref.subview %alloc_20[0, 0, 0, 0] [8, 32, 1024, 64] [1, 1, 1, 1] : memref<8x32x1024x128xbf16> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1]>>
    memref.copy %alloc_19, %subview_21 : memref<8x32x1024x64xbf16> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1]>>
    %subview_22 = memref.subview %alloc_20[0, 0, 0, 64] [8, 32, 1024, 64] [1, 1, 1, 1] : memref<8x32x1024x128xbf16> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1], offset: 64>>
    memref.copy %subview, %subview_22 : memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1]>> to memref<8x32x1024x64xbf16, strided<[4194304, 131072, 128, 1], offset: 64>>
    linalg.generic {indexing_maps = [#map9, #map10, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_20, %expand_shape_16 : memref<8x32x1024x128xbf16>, memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>) outs(%alloc_6 : memref<8x32x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    linalg.generic {indexing_maps = [#map9, #map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_17, %alloc_6 : memref<8x32x1024x128xbf16>, memref<8x32x1024x128xbf16>) outs(%alloc_6 : memref<8x32x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.addf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    %alloc_23 = memref.alloc() {alignment = 64 : i64} : memref<8x8x1024x128xbf16>
    linalg.generic {indexing_maps = [#map9, #map10, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_12, %expand_shape_15 : memref<8x8x1024x128xbf16>, memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>) outs(%alloc_23 : memref<8x8x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    %subview_24 = memref.subview %alloc_12[0, 0, 0, 0] [8, 8, 1024, 64] [1, 1, 1, 1] : memref<8x8x1024x128xbf16> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1]>>
    %subview_25 = memref.subview %alloc_12[0, 0, 0, 64] [8, 8, 1024, 64] [1, 1, 1, 1] : memref<8x8x1024x128xbf16> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1], offset: 64>>
    %alloc_26 = memref.alloc() {alignment = 64 : i64} : memref<8x8x1024x64xbf16>
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%subview_25 : memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1], offset: 64>>) outs(%alloc_26 : memref<8x8x1024x64xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      %0 = arith.negf %in : bf16
      linalg.yield %0 : bf16
    }
    %alloc_27 = memref.alloc() {alignment = 64 : i64} : memref<8x8x1024x128xbf16>
    %subview_28 = memref.subview %alloc_27[0, 0, 0, 0] [8, 8, 1024, 64] [1, 1, 1, 1] : memref<8x8x1024x128xbf16> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1]>>
    memref.copy %alloc_26, %subview_28 : memref<8x8x1024x64xbf16> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1]>>
    %subview_29 = memref.subview %alloc_27[0, 0, 0, 64] [8, 8, 1024, 64] [1, 1, 1, 1] : memref<8x8x1024x128xbf16> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1], offset: 64>>
    memref.copy %subview_24, %subview_29 : memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1]>> to memref<8x8x1024x64xbf16, strided<[1048576, 131072, 128, 1], offset: 64>>
    linalg.generic {indexing_maps = [#map9, #map10, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_27, %expand_shape_16 : memref<8x8x1024x128xbf16>, memref<1x1x1x128xbf16, strided<[?, ?, ?, ?], offset: ?>>) outs(%alloc_12 : memref<8x8x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    linalg.generic {indexing_maps = [#map9, #map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_23, %alloc_12 : memref<8x8x1024x128xbf16>, memref<8x8x1024x128xbf16>) outs(%alloc_12 : memref<8x8x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.addf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    %alloc_30 = memref.alloc() {alignment = 64 : i64} : memref<8x8x4x1024x128xbf16>
    %alloc_31 = memref.alloc() {alignment = 64 : i64} : memref<8x8x4x1024x128xbf16>
    linalg.generic {indexing_maps = [#map11, #map12], iterator_types = ["parallel", "parallel", "parallel", "parallel", "parallel"]} ins(%alloc_12 : memref<8x8x1024x128xbf16>) outs(%alloc_31 : memref<8x8x4x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %collapse_shape = memref.collapse_shape %alloc_31 [[0], [1, 2], [3], [4]] : memref<8x8x4x1024x128xbf16> into memref<8x32x1024x128xbf16>
    linalg.generic {indexing_maps = [#map11, #map12], iterator_types = ["parallel", "parallel", "parallel", "parallel", "parallel"]} ins(%alloc_14 : memref<8x8x1024x128xbf16>) outs(%alloc_30 : memref<8x8x4x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %alloc_32 = memref.alloc() {alignment = 64 : i64} : memref<8x32x128x1024xbf16>
    linalg.generic {indexing_maps = [#map13, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%collapse_shape : memref<8x32x1024x128xbf16>) outs(%alloc_32 : memref<8x32x128x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %collapse_shape_33 = memref.collapse_shape %alloc_6 [[0, 1], [2], [3]] : memref<8x32x1024x128xbf16> into memref<256x1024x128xbf16>
    %collapse_shape_34 = memref.collapse_shape %alloc_32 [[0, 1], [2], [3]] : memref<8x32x128x1024xbf16> into memref<256x128x1024xbf16>
    %alloc_35 = memref.alloc() {alignment = 64 : i64} : memref<256x1024x1024xbf16>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%cst : bf16) outs(%alloc_35 : memref<256x1024x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%collapse_shape_33, %collapse_shape_34 : memref<256x1024x128xbf16>, memref<256x128x1024xbf16>) outs(%alloc_35 : memref<256x1024x1024xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %expand_shape_36 = memref.expand_shape %alloc_35 [[0, 1], [2], [3]] output_shape [8, 32, 1024, 1024] : memref<256x1024x1024xbf16> into memref<8x32x1024x1024xbf16>
    %alloc_37 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x1024xbf16>
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%expand_shape_36 : memref<8x32x1024x1024xbf16>) outs(%alloc_37 : memref<8x32x1024x1024xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      %0 = arith.truncf %cst_2 : f64 to bf16
      %1 = arith.mulf %in, %0 : bf16
      linalg.yield %1 : bf16
    }
    linalg.generic {indexing_maps = [#map9, #map14, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_37, %arg3 : memref<8x32x1024x1024xbf16>, memref<1x32x1x1xbf16, strided<[?, ?, ?, ?], offset: ?>>) outs(%alloc_37 : memref<8x32x1024x1024xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.addf %in, %in_50 : bf16
      linalg.yield %0 : bf16
    }
    %alloc_38 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x1024xf32>
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_37 : memref<8x32x1024x1024xbf16>) outs(%alloc_38 : memref<8x32x1024x1024xf32>) {
    ^bb0(%in: bf16, %out: f32):
      %0 = arith.extf %in : bf16 to f32
      linalg.yield %0 : f32
    }
    %alloc_39 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024xi64>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%c0_i64 : i64) outs(%alloc_39 : memref<8x32x1024xi64>) {
    ^bb0(%in: i64, %out: i64):
      linalg.yield %in : i64
    }
    %alloc_40 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024xf32>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%cst_0 : f32) outs(%alloc_40 : memref<8x32x1024xf32>) {
    ^bb0(%in: f32, %out: f32):
      linalg.yield %in : f32
    }
    linalg.generic {indexing_maps = [#map9, #map7, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%alloc_38 : memref<8x32x1024x1024xf32>) outs(%alloc_40, %alloc_39 : memref<8x32x1024xf32>, memref<8x32x1024xi64>) {
    ^bb0(%in: f32, %out: f32, %out_50: i64):
      %0 = linalg.index 3 : index
      %1 = arith.index_cast %0 : index to i64
      %2 = arith.maximumf %in, %out : f32
      %3 = arith.cmpf ogt, %in, %out : f32
      %4 = arith.select %3, %1, %out_50 : i64
      linalg.yield %2, %4 : f32, i64
    }
    %expand_shape_41 = memref.expand_shape %alloc_40 [[0], [1], [2, 3]] output_shape [8, 32, 1024, 1] : memref<8x32x1024xf32> into memref<8x32x1024x1xf32>
    linalg.generic {indexing_maps = [#map9, #map15, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_38, %expand_shape_41 : memref<8x32x1024x1024xf32>, memref<8x32x1024x1xf32>) outs(%alloc_38 : memref<8x32x1024x1024xf32>) {
    ^bb0(%in: f32, %in_50: f32, %out: f32):
      %0 = arith.subf %in, %in_50 : f32
      linalg.yield %0 : f32
    }
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_38 : memref<8x32x1024x1024xf32>) outs(%alloc_38 : memref<8x32x1024x1024xf32>) {
    ^bb0(%in: f32, %out: f32):
      %0 = math.exp %in : f32
      linalg.yield %0 : f32
    }
    %alloc_42 = memref.alloc() {alignment = 64 : i64} : memref<8x32x1024x1xf32>
    linalg.generic {indexing_maps = [#map16, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%cst_1 : f32) outs(%alloc_42 : memref<8x32x1024x1xf32>) {
    ^bb0(%in: f32, %out: f32):
      linalg.yield %in : f32
    }
    linalg.generic {indexing_maps = [#map9, #map15], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%alloc_38 : memref<8x32x1024x1024xf32>) outs(%alloc_42 : memref<8x32x1024x1xf32>) {
    ^bb0(%in: f32, %out: f32):
      %0 = arith.addf %in, %out : f32
      linalg.yield %0 : f32
    }
    linalg.generic {indexing_maps = [#map9, #map15, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_38, %alloc_42 : memref<8x32x1024x1024xf32>, memref<8x32x1024x1xf32>) outs(%alloc_38 : memref<8x32x1024x1024xf32>) {
    ^bb0(%in: f32, %in_50: f32, %out: f32):
      %0 = arith.divf %in, %in_50 : f32
      linalg.yield %0 : f32
    }
    linalg.generic {indexing_maps = [#map9, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%alloc_38 : memref<8x32x1024x1024xf32>) outs(%alloc_37 : memref<8x32x1024x1024xbf16>) {
    ^bb0(%in: f32, %out: bf16):
      %0 = arith.truncf %in : f32 to bf16
      linalg.yield %0 : bf16
    }
    %collapse_shape_43 = memref.collapse_shape %alloc_37 [[0, 1], [2], [3]] : memref<8x32x1024x1024xbf16> into memref<256x1024x1024xbf16>
    %collapse_shape_44 = memref.collapse_shape %alloc_30 [[0, 1, 2], [3], [4]] : memref<8x8x4x1024x128xbf16> into memref<256x1024x128xbf16>
    %alloc_45 = memref.alloc() {alignment = 64 : i64} : memref<256x1024x128xbf16>
    linalg.generic {indexing_maps = [#map4, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%cst : bf16) outs(%alloc_45 : memref<256x1024x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%collapse_shape_43, %collapse_shape_44 : memref<256x1024x1024xbf16>, memref<256x1024x128xbf16>) outs(%alloc_45 : memref<256x1024x128xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %expand_shape_46 = memref.expand_shape %alloc_45 [[0, 1], [2], [3]] output_shape [8, 32, 1024, 128] : memref<256x1024x128xbf16> into memref<8x32x1024x128xbf16>
    %alloc_47 = memref.alloc() {alignment = 64 : i64} : memref<8x1024x32x128xbf16>
    linalg.generic {indexing_maps = [#map8, #map9], iterator_types = ["parallel", "parallel", "parallel", "parallel"]} ins(%expand_shape_46 : memref<8x32x1024x128xbf16>) outs(%alloc_47 : memref<8x1024x32x128xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    %collapse_shape_48 = memref.collapse_shape %alloc_47 [[0], [1], [2, 3]] : memref<8x1024x32x128xbf16> into memref<8x1024x4096xbf16>
    linalg.generic {indexing_maps = [#map, #map1], iterator_types = ["parallel", "parallel"]} ins(%arg7 : memref<4096x4096xbf16, strided<[?, ?], offset: ?>>) outs(%alloc : memref<4096x4096xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map2, #map3], iterator_types = ["parallel", "parallel", "parallel"]} ins(%alloc : memref<4096x4096xbf16>) outs(%alloc_3 : memref<8x4096x4096xbf16>) {
    ^bb0(%in: bf16, %out: bf16):
      linalg.yield %in : bf16
    }
    linalg.generic {indexing_maps = [#map5, #map6, #map7], iterator_types = ["parallel", "parallel", "parallel", "reduction"]} ins(%collapse_shape_48, %alloc_3 : memref<8x1024x4096xbf16>, memref<8x4096x4096xbf16>) outs(%alloc_4 : memref<8x1024x4096xbf16>) {
    ^bb0(%in: bf16, %in_50: bf16, %out: bf16):
      %0 = arith.mulf %in, %in_50 : bf16
      %1 = arith.addf %out, %0 : bf16
      linalg.yield %1 : bf16
    }
    %cast = memref.cast %alloc_4 : memref<8x1024x4096xbf16> to memref<8x1024x4096xbf16, strided<[?, ?, ?], offset: ?>>
    %cast_49 = memref.cast %alloc_37 : memref<8x32x1024x1024xbf16> to memref<8x32x1024x1024xbf16, strided<[?, ?, ?, ?], offset: ?>>
    return %alloc_4, %alloc_37 : memref<8x1024x4096xbf16>, memref<8x32x1024x1024xbf16>
  }
}
